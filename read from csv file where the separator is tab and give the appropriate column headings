#Program to read from csv file where the separator is tab  and give the appropriate column headings
import pandas as pd
deptdf=pd.read_csv("Location",sep='\t',names=[Names of columns in CSV file])
print(deptdf)
